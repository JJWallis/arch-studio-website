// Given an integer num
// repeatedly add all its digits until the result has only one digit + return it
// edgcase --- 0 <= num <= Math.pow(2, 31) - 1

// Input: num = 38
// Output: 2
// Explanation: The process is
// 38 --> 3 + 8 --> 11
// 11 --> 1 + 1 --> 2

function addDigits(num: number) {
   if (num <= 0) return
}

console.log(addDigits(38))
